'use strict'

// Promise is a JavaScript object for asyncronous operation.
// State: pending -> fulfilled or rejected
// Producer or Cunsumer

// 1. Producer
// when new producer is created, the executor runs automatically.
const promise = new Promise()